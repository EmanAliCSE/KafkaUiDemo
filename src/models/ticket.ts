export interface TicketBooking {
    id: string;
    // userId: string;
    quantity: number;
    bookingTime: string;
    status: string;
    processedTime?: string;
    // confirmationMessage?: string;
  }
  