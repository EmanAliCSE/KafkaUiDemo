import { environment } from "../environments/environment";

export const BASE_URL = `${environment.apiUrl}`;
export const BOOKING_URL = 'Booking';
export const LOGS_URL = 'logs';
export const OUTBOX_URL = 'OutboxMsgs';
